<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:views="clr-namespace:NS.MBX_amarin.View"
             x:Class="NS.MBX_amarin.View.CtaDestinoView">

    <AbsoluteLayout BackgroundColor="White">
        <StackLayout AbsoluteLayout.LayoutBounds="0,0,1,1" 
                         AbsoluteLayout.LayoutFlags="All">
            <StackLayout x:Name="layoutOtrasCuentas" VerticalOptions="CenterAndExpand">
                <Label Text="Número de cuenta destino"></Label>
                <Entry x:Name="entNumCuenta" VerticalOptions="CenterAndExpand" />
                <Button Text="Siguiente" VerticalOptions="End" Clicked="BtnSgt_OnClicked"></Button>

            </StackLayout>

            <StackLayout x:Name="layoutMisCuentas" VerticalOptions="CenterAndExpand">
                <ListView x:Name="lsvCtas" ItemTapped="LsvCtas_ItemTapped" CachingStrategy="RecycleElement">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <ViewCell.View>
                                    <StackLayout Orientation="Horizontal">
                                        <StackLayout BackgroundColor="Transparent" HorizontalOptions="Start"></StackLayout>
                                        <StackLayout BackgroundColor="Transparent" HorizontalOptions="CenterAndExpand" Orientation="Vertical">
                                            <Label Text="{Binding NombreCta}" VerticalOptions="Start"></Label>
                                            <Label Text="{Binding SaldoDisponible}" VerticalOptions="CenterAndExpand"></Label>
                                            <Label Text="Saldo Disponible" VerticalOptions="End"></Label>
                                        </StackLayout>
                                        <StackLayout BackgroundColor="Transparent" HorizontalOptions="End"></StackLayout>
                                    </StackLayout>
                                </ViewCell.View>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </StackLayout>

        <views:NavBarView x:Name="navBar" AbsoluteLayout.LayoutBounds="0,1,1,60" 
                           AbsoluteLayout.LayoutFlags="PositionProportional,WidthProportional"/>
    </AbsoluteLayout>

    <!--<ListView x:Name="MyListView"
            ItemsSource="{Binding Items}"
            ItemTapped="Handle_ItemTapped"
            CachingStrategy="RecycleElement" IsVisible="False">
            
    --><!--Built in Cells--><!--
    <ListView.ItemTemplate>
        <DataTemplate>
            <TextCell Text="{Binding .}" />
        </DataTemplate>
    </ListView.ItemTemplate>-->
    
    <!--Custom View Cells-->
    <!--
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell>
          <StackLayout>
            <Label Text="{Binding Text}" 
                   Style="{DynamicResource ListItemTextStyle}" />
            <Label Text="{Binding Detail}" 
                   Style="{DynamicResource ListItemDetailTextStyle}"/>
          </StackLayout>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
    -->
  <!--</ListView>
    <StackLayout VerticalOptions="Start">
        
    </StackLayout>
    <StackLayout VerticalOptions="FillAndExpand" Padding="5,5,5,5">
        <Label Text="Número de cuenta destino"></Label>
        <Entry HorizontalOptions="FillAndExpand"></Entry>
        <Button Text="Siguiente" VerticalOptions="End" Clicked="BtnSgt_OnClicked"></Button>
    </StackLayout>-->
</ContentPage>
